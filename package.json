{
  "name": "xcski",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/add-decision-board.mjs && node scripts/generate-trail-pages.mjs && node scripts/inject-site-tags.mjs",
    "test": "npm run build && node scripts/check-build.mjs"
  }
}
